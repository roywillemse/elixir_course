[
  locals_without_parens: [
    # &Kernel.match?/2
    match?: 2,
    # Introduced by the UUID resolver convention.
    fields: 1,
    option: 3,
    option: 4,
    column: 3,
    column: 4
  ],
  import_deps: [:ecto, :plug, :absinthe],
  inputs: ["*.{ex,exs}", "{config,lib,test}/**/*.{ex,exs}"]
]
